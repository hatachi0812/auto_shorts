{
  "name": "alphacut-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && uvicorn main:app --reload --port 8000",
    "dev:web": "cd apps/web && npm run dev",
    "install:web": "cd apps/web && npm install",
    "install:api": "cd apps/api && pip install -r requirements.txt"
  }
}
